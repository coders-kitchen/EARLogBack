apply plugin: 'ear'

allprojects {
	repositories {
		mavenLocal()
		mavenCentral()
	}
}

subprojects {
	apply plugin: 'java'

	dependencies {
		compile 'javax:javaee-api:6.0'
	}
}

dependencies {
  deploy project(path: ':war', configuration: 'archives'), project(path: ':slf4j')
}

task gW(type : Wrapper) {

}